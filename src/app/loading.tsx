const DefaultLoader = () => {
  return (
    <section className="relative flex items-center justify-center w-full h-screen bg-gradient-to-br from-primary/10 to-secondary/10">
      <div className="loader"></div>
    </section>
  );
};
export default DefaultLoader;
